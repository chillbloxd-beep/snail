import{state,pingText}from"../core/state.js";import{command}from"../core/worker.js";import{escapeHtml}from"../core/diagnostics.js";export function render(root){root.innerHTML=`<section class="card"><h2>Health</h2><div class="grid">${[...state.devices.values()].map(d=>`<div class="device-card"><h3>${escapeHtml(d.deviceName||d.deviceId)}</h3><p>Type: ${escapeHtml(d.type)}</p><p>Device ping: ${pingText(state.ping.device.get(d.deviceId)||[])}</p><p>Live ping: ${pingText(state.ping.live.get(d.deviceId)||[])}</p><div class="controls"><button data-health="${d.deviceId}">Get Health</button><button data-ping="${d.deviceId}">Ping</button></div></div>`).join("")||"<p>No devices.</p>"}</div></section>`;root.querySelectorAll("[data-health]").forEach(b=>b.onclick=()=>command("GET_AGENT_HEALTH",{},b.dataset.health));root.querySelectorAll("[data-ping]").forEach(b=>b.onclick=()=>command("PING",{pingId:crypto.randomUUID(),sentAt:Date.now()},b.dataset.ping));}